import 'package:flutter/material.dart';

class RightSideDetailScreen extends StatelessWidget {
  const RightSideDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Right Side Detail Screen'),
      ),
    );
  }
}
